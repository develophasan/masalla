{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "CI=false REACT_APP_BACKEND_URL=$REACT_APP_BACKEND_URL yarn install && CI=false REACT_APP_BACKEND_URL=$REACT_APP_BACKEND_URL yarn build"
  },
  "deploy": {
    "startCommand": "npx serve -s build -l ${PORT:-3000}",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
